/**
 * Folder entry: use `import("./GamePlay")` so the editor opens the folder route.
 * Component implementation lives in `GamePlay.js` (PascalCase — matches Git; avoids Windows save/case confusion).
 */
export { default } from './GamePlay';
